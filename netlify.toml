# netlify.toml

[build]
  # CRITICAL FIX: Vite builds to 'dist', not 'build'
  publish = "dist" 
  
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@google/genai"]