# netlify.toml

[build]
  # Dossier où votre application React est compilée
  publish = "build" 
  
[functions]
  # Chemin vers vos fonctions
  directory = "netlify/functions"
  
  # Configuration pour le bundling (C'EST LA CLÉ pour les imports ES6 et GenAI)
  node_bundler = "esbuild"