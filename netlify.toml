[build]
  command = "npm run build"
  publish = "dist" 
  
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  # Inclure explicitement la dépendance GenAI
  external_node_modules = ["@google/genai"]
  # Inclure explicitement le dossier src pour que les fonctions trouvent constants.js
  included_files = ["src/**"]